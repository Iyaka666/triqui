//Modifica el tablero a una cantidad determinada de columnas y filas para cada celda
const $table = document.querySelectorAll(".table-game tr td");
console.log($table);
/*
$table.forEach((el) => {
    const ROWS = 5;
    const COLS = 5;
    //el.setAttribute("rowspan", ROWS);
    //el.setAttribute("colspan", COLS);
});
*/     